import React from 'react';
import PropTypes from 'prop-types';

const BoardsOverview = ({ className }) => (
  <div className={className}>
    BoardsOverview
  </div>
);

BoardsOverview.propTypes = {
  className: PropTypes.string,
};

export default BoardsOverview;